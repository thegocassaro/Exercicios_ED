{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Opacos_Genericos/main",
            "args": [
                // "<", "${workspaceFolder}/Opacos_Genericos/entrada"
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "compilacao"
        },
    ]
}