{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/FilaPrio_Heap/main",
            "args": [
                "<", "${workspaceFolder}/FilaPrio_Heap/input1"
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "compilacao"
        },
    ]
}