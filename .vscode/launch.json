{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/ArraysCirculares/main",
            "args": [
                "<", "${workspaceFolder}/ArraysCirculares/input"
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "compilacao"
        },
    ]
}